/* GitHub Swimwear OPC Style.css */

:root {
    --primary-color: #f4f1ea; /* Soft Sand */
    --accent-color: #333d42; /* Navy Grey */
    --text-color: #2c3e50;
    --white: #ffffff;
    --spacing: 20px;
}

body {
    font-family: 'Helvetica Neue', Arial, sans-serif;
    line-height: 1.6;
    color: var(--text-color);
    background-color: var(--white);
    margin: 0;
}

header {
    background: var(--white);
    border-bottom: 1px solid #eee;
    padding: 1rem 2rem;
    position: sticky;
    top: 0;
    z-index: 1000;
}

nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
}

.logo {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 2px;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 1.5rem;
}

nav a {
    text-decoration: none;
    color: var(--text-color);
    font-weight: 500;
    font-size: 0.9rem;
}

.hero {
    background: var(--primary-color);
    height: 60vh;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 0 2rem;
}

.hero-content h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    font-weight: 300;
    letter-spacing: 1px;
}

.hero-content p {
    font-size: 1.2rem;
    color: #666;
    margin-bottom: 2rem;
}

.btn {
    background: var(--accent-color);
    color: var(--white);
    padding: 0.8rem 2rem;
    text-decoration: none;
    border-radius: 5px;
    font-size: 1rem;
}

.collection {
    padding: 4rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
}

.collection h2 {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 3rem;
    font-weight: 300;
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 3rem;
}

.product-card {
    text-align: center;
    transition: transform 0.3s ease;
}

.product-card:hover {
    transform: translateY(-5px);
}

.product-card img {
    max-width: 100%;
    height: auto;
    margin-bottom: 1.5rem;
    border-radius: 8px;
}

.product-card h3 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
    font-weight: 500;
}

.product-card .price {
    font-weight: bold;
    font-size: 1.1rem;
    display: block;
    margin-top: 0.5rem;
}

footer {
    background: #f9f9f9;
    padding: 3rem 2rem;
    text-align: center;
    border-top: 1px solid #eee;
}

@media (max-width: 768px) {
    .hero-content h1 { font-size: 2rem; }
    nav ul { display: none; }
}
